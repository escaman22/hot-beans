import { Routes } from '@angular/router';
import { CoffeeMapComponent } from './coffee-map-component/coffee-map-component';

export const routes: Routes = [{ path: '', component: CoffeeMapComponent }];
